# AGENTS.md

## 1. このファイルの目的

このファイルは、報告書作成ツールに対してCodexがリファクタリングおよび機能修正を行う際の作業ルールを定義する。

Codexは作業開始前にこのルールを確認し、すべての変更で遵守すること。

今回の作業目的は、次の2点である。

1. 既存仕様を維持したまま、コードの可読性・保守性・再利用性を向上させる
2. 確認済みの不具合および指定された機能を修正する

大規模な再設計や新機能追加を目的としない。

---

# 2. ツールの概要

このツールは、飲食店のPOS売上CSVと各種マスタCSVを読み込み、以下の月次報告書をExcel形式で出力する。

* 店舗別報告書
* エリア別報告書
* 全体報告書

主な処理は次のとおり。

1. 設定値の取得
2. 対象年月の取得
3. 売上CSVの読み込み
4. 各種マスタCSVの読み込み
5. 入力データのバリデーション
6. 店舗別集計
7. エリア別集計
8. 全体集計
9. Excel報告書の出力
10. 処理結果またはエラーの表示

---

# 3. リファクタリング対象ファイル

以下を今回の主な対象ファイルとする。

* `aggregator.py`
* `config.py`
* `csv_loader.py`
* `main.py`
* `master_loader.py`
* `report_writer.py`
* `validation_rules.py`
* `validator.py`
* `test_aggregator.py`
* `test_validator.py`

実際のディレクトリ構成に応じて、`app/` や `tests/` などのパスを読み替えること。

対象外のファイルは、変更の必要性が明確でない限り変更しないこと。

関連ファイルの変更が必要になった場合は、変更前に理由と影響範囲を説明すること。

---

# 4. 最優先ルール

## 4.1 既存仕様を壊さない

現在正常に動作している以下の処理を維持すること。

* 売上CSVの読み込み
* マスタCSVの読み込み
* 店舗別集計
* エリア別集計
* 全体集計
* 店舗報告書の出力
* エリア報告書の出力
* 全体報告書の出力
* 各種バリデーション
* エラー発生時の処理中断
* 出力ファイル名の生成
* Excelテンプレートの利用
* Excelセルへの出力位置
* 既存テストで保証されている動作

処理結果や出力内容が変わる可能性のある変更を、独断で行わないこと。

---

## 4.2 必要のない箇所は変更しない

リファクタリング対象として確認した結果、現在のコードで問題がない場合は変更しないこと。

変更量を増やすことを目的にしない。

次のような変更は避けること。

* 意味のない変数名変更
* 動作に影響しない大量の整形
* 不要な関数分割
* 不要な共通化
* 不要なクラス化
* 不要なファイル分割
* 好みだけを理由にした書き換え
* 既存実装を全面的に置き換える変更

---

## 4.3 変更前にコードを理解する

対象ファイルを変更する前に、必ず次を確認すること。

* ファイルの役割
* 各関数の役割
* 関数の呼び出し元
* 関数の呼び出し先
* 入力データ
* 戻り値
* 例外処理
* 他ファイルへの影響
* 関連するテスト
* 業務ルール
* Excel出力との関連
* 変更が仕様変更にならないか

コードの一部分だけを見て判断せず、関連処理の流れを確認すること。

---

# 5. 今回実施する機能修正

## 5.1 対象年月の固定値を廃止する

現在、`main.py` の対象年月が次のような固定値になっている場合は修正する。

```python
target_month = "202606"
```

対象年月を実行時に変更できる構造にすること。

原則として、コマンドライン引数から受け取る方式を優先する。

実行例：

```bash
python -m app.main --target-month 202606
```

現在のプロジェクト構成により、次のような実行方法が適切な場合は、その理由を説明したうえで採用してよい。

```bash
python app/main.py --target-month 202606
```

---

## 5.2 対象年月の入力ルール

対象年月は、6桁の `YYYYMM` 形式とする。

正常例：

```text
202606
202612
202701
```

異常例：

```text
2026-06
2026/06
20266
202600
202613
abcdef
```

形式だけではなく、月が `01` から `12` の範囲であることも確認すること。

対象年月が不正な場合は、CSV読み込みや報告書作成を開始せず、利用者が原因を理解できるエラーメッセージを表示すること。

対象年月について、次の責務を可能な範囲で分離する。

* コマンドライン引数の取得
* `YYYYMM` 形式の検証
* 有効な年月であることの検証
* アプリケーション処理への受け渡し

入力処理を分離し、単体テスト可能な構造にすること。

---

## 5.3 営業利益率の表示不具合を修正する

現在、Excel報告書で営業利益率が3000%以上になる場合がある。

この問題を、今回の正式な不具合修正対象とする。

表面的に値を100で割るだけではなく、計算からExcel表示までの処理経路を確認し、原因を特定して修正すること。

確認する処理は次のとおり。

1. 営業利益の計算
2. 営業利益率の計算
3. 集計結果として保持される値
4. `report_writer.py` へ渡される値
5. Excelセルへ書き込まれる値
6. Excelセルの表示形式
7. 店舗報告書
8. エリア報告書
9. 全体報告書
10. 関連するテスト

---

## 5.4 営業利益率の計算ルール

営業利益率は、原則として次の式で計算する。

```python
operating_profit_margin = operating_profit / net_sales
```

Excel側でパーセント表示を行う場合、Python側では100を掛けないこと。

例：

```python
net_sales = 1_000_000
operating_profit = 300_000

operating_profit_margin = operating_profit / net_sales
```

この場合、Python側の値は次のとおり。

```text
0.3
```

Excelセルの表示形式がパーセント形式であれば、次のように表示される。

```text
30%
```

次のようにPython側で100を掛けた値を、Excelのパーセント形式のセルへ書き込まないこと。

```python
operating_profit_margin = operating_profit / net_sales * 100
```

この場合、Python側の値が `30` となり、Excel側でも100倍表示されることで、`3000%` になる可能性がある。

---

## 5.5 Excelのパーセント表示

営業利益率のExcelセルには、既存フォーマットに合ったパーセント表示形式を設定する。

小数点以下を表示しない場合：

```python
worksheet["F21"].number_format = "0%"
```

小数点以下1桁を表示する場合：

```python
worksheet["F21"].number_format = "0.0%"
```

使用するセル番地は、現在の報告書仕様と実装を確認すること。

現在の報告書仕様では、営業利益率の出力先は `F21` を想定しているが、実際のテンプレートとコードを確認してから修正すること。

---

## 5.6 売上が0の場合の処理

税抜売上が0の場合は、ゼロ除算が発生しないようにすること。

例：

```python
if net_sales == 0:
    operating_profit_margin = 0
else:
    operating_profit_margin = operating_profit / net_sales
```

ただし、売上0の場合に次のどちらを採用するかは、既存実装・既存テスト・他の比率項目の扱いを確認すること。

* `0` とする
* `None` または空欄とする

既存仕様から判断できない場合は、独断で決定せず報告すること。

---

## 5.7 原価率と人件費率も確認する

営業利益率だけでなく、同じ仕組みを使用している次の項目も確認すること。

* 原価率
* 人件費率

確認項目：

* Python側で100を掛けていないか
* Excel側でパーセント形式になっているか
* PythonとExcelで二重に100倍していないか
* 店舗・エリア・全体で計算ルールが統一されているか
* 売上0の場合にゼロ除算が発生しないか

問題がない項目は変更しないこと。

問題がある場合は、原因と影響範囲を説明したうえで修正すること。

---

# 6. 可読性に関するルール

## 6.1 分かりやすい英単語を使う

変数名、関数名、定数名には、役割を理解しやすい英単語を使用すること。

意味が不明確な略語や、一時的な名前を避けること。

避ける例：

```python
df
tmp
val
res
data2
lst
dict1
x
y
```

推奨例：

```python
sales_data
store_master
area_master
validation_errors
target_month
report_output_path
aggregated_results
operating_profit
operating_profit_margin
```

ただし、`DataFrame` を扱うプロジェクト内で `df` が一貫して使用され、前後の文脈から意味が明確な場合などは、機械的にすべて変更しないこと。

名前を変更する場合は、呼び出し元、テスト、キーワード引数への影響も確認すること。

---

## 6.2 関数名から処理内容が分かるようにする

関数名は、処理内容を表す動詞を含めること。

推奨例：

```python
load_sales_csv()
load_store_master()
validate_required_columns()
validate_target_month()
aggregate_store_sales()
calculate_operating_profit_margin()
write_store_report()
build_report_filename()
```

次のような曖昧な名前は、役割を確認したうえで改善を検討すること。

```python
process()
execute()
handle()
do_data()
run_data()
```

アプリケーション全体の実行入口である `main()` は使用してよい。

---

## 6.3 1つの関数に役割を集中させすぎない

1つの関数が複数の異なる責務を持っている場合は、分割を検討すること。

分離を検討する処理：

* ファイルパスを作る
* CSVを読み込む
* データを検証する
* データを集計する
* 比率を計算する
* Excelへ書き込む
* 出力ファイルを保存する
* エラーメッセージを表示する

ただし、関数を細かく分割しすぎて、処理の流れが追いにくくならないようにすること。

数行という理由だけで関数を分割しないこと。

業務上の意味とテストのしやすさを基準に判断すること。

---

## 6.4 `main.py` では全体の流れを見えるようにする

`main.py` は、報告書作成処理の流れを確認できる構造にすること。

次の流れが読み取れるようにする。

1. コマンドライン引数の取得
2. 対象年月の検証
3. 設定値の取得
4. 売上CSVの読み込み
5. マスタCSVの読み込み
6. 入力データのバリデーション
7. 店舗別集計
8. エリア別集計
9. 全体集計
10. 店舗報告書の出力
11. エリア報告書の出力
12. 全体報告書の出力
13. 完了メッセージの表示
14. 例外処理

処理の詳細は適切なモジュールへ委譲し、`main.py` にすべての実装を集中させないこと。

---

# 7. コメントとdocstringのルール

## 7.1 コメントには「なぜ」を書く

コードをそのまま日本語に置き換えただけのコメントは追加しないこと。

不要な例：

```python
# CSVを読み込む
sales_data = load_sales_csv(csv_path)
```

理由や業務上の背景が必要な箇所にコメントを書くこと。

例：

```python
# 店舗コードの先頭ゼロを保持するため、文字列型として読み込む
```

例：

```python
# Excel側でパーセント表示するため、ここでは100を掛けず小数値を保持する
```

---

## 7.2 コメントを付ける対象

次のような箇所には、必要に応じてコメントを付けること。

* 業務ルールを含む処理
* 税率の有効期間判定
* 店舗数・エリア数のチェック理由
* CSVファイル数の判定理由
* 営業日欠損の判定
* 店舗・エリア・全体集計の違い
* Excelの特定セルへ出力する理由
* 比率を小数値として保持する理由
* ファイル名の生成ルール
* 一見すると不要に見える処理
* 将来変更が想定される処理
* 例外処理の意図

すべての行にコメントを付けないこと。

コードを読めば分かる内容に、重複したコメントを追加しないこと。

---

## 7.3 docstringを適切に使用する

外部モジュールから呼び出される関数や、業務ルールを含む関数には、必要に応じてdocstringを追加すること。

docstringには、必要な範囲で次を記載する。

* 処理の概要
* 引数
* 戻り値
* 発生する例外
* 業務上の注意点

例：

```python
def calculate_operating_profit_margin(
    operating_profit: float,
    net_sales: float,
) -> float:
    """営業利益率を小数形式で計算する。

    Excel側でパーセント表示するため、100は掛けない。

    Args:
        operating_profit: 営業利益。
        net_sales: 税抜売上。

    Returns:
        営業利益率。30%の場合は0.3を返す。
    """
```

過剰に長いdocstringや、コードと同じ内容を繰り返すdocstringは避けること。

---

# 8. 共通化のルール

## 8.1 重複処理を確認する

次のような処理に重複がある場合は、共通化を検討すること。

* CSVファイルパスの生成
* 対象年月の形式チェック
* 出力ディレクトリの生成
* 出力ファイル名の生成
* Excelテンプレートの読み込み
* Excelセルへの値設定
* 店舗・エリア・全体報告書の共通処理
* 比率計算
* ゼロ除算対策
* 数値変換
* NULL判定
* マスタコードの存在確認
* エラーメッセージの生成

---

## 8.2 共通化しすぎない

コードが似ているだけで無理に共通化しないこと。

次の場合は、別処理のまま残してよい。

* 業務上の意味が異なる
* 将来別々に変更される可能性が高い
* 共通関数の引数が増えすぎる
* 多数のフラグが必要になる
* 関数名が曖昧になる
* 呼び出し側が理解しにくくなる
* 条件分岐がかえって複雑になる

重複を完全になくすことよりも、変更箇所と処理の意味が分かりやすいことを優先すること。

---

# 9. 設定値と定数のルール

## 9.1 マジックナンバーを確認する

業務上意味のある固定値は、必要に応じて定数または設定値として管理すること。

例：

```python
TARGET_MONTH_FORMAT = "%Y%m"
REPORT_FILE_EXTENSION = ".xlsx"
```

ただし、一度しか使用せず意味が明確な値まで、すべて定数化しないこと。

---

## 9.2 設定値の配置

設定値は用途に応じて配置すること。

* アプリケーション全体で使う設定：`config.py`
* 特定モジュールだけで使う値：対象モジュール内の定数
* 実行時に変更する値：コマンドライン引数または関数の引数
* Excelセル番地：既存設計を確認し、現在の管理方法に合わせる

対象年月のように実行ごとに変わる値を、固定設定として埋め込まないこと。

---

# 10. バリデーションのルール

## 10.1 既存バリデーションを維持する

次の既存バリデーションを削除しないこと。

* 必須カラムチェック
* 対象月チェック
* 店舗数チェック
* 店舗コード整合チェック
* CSVファイル数チェック
* 営業日欠損チェック
* エリアコード整合チェック
* エリア数チェック
* NULLチェック
* 数値項目チェック
* 重複チェック
* 税率有効期間チェック

店舗とエリアの関連については、売上CSVにエリアコードがないため、店舗マスタなどの既存情報を利用した整合確認の仕様を維持すること。

---

## 10.2 バリデーションの責務

可能な範囲で次の責務を整理すること。

### `validation_rules.py`

* 個別の検証ルール
* 判定条件
* 個別エラーメッセージの生成

### `validator.py`

* 各検証ルールの呼び出し
* 検証順序の管理
* エラーの収集
* 検証結果の返却
* エラー時の処理制御

ただし、現在の構造で十分に分かりやすい場合は、無理に移動や分割を行わないこと。

---

## 10.3 エラーを握りつぶさない

次のような実装は禁止する。

```python
try:
    ...
except Exception:
    pass
```

例外を捕捉する場合は、次のいずれかを行うこと。

* 利用者が原因を理解できるメッセージを表示する
* 必要な情報を付けて例外を再送出する
* 処理を安全に中断する
* 呼び出し元へ失敗を通知する

想定できる例外については、可能な限り具体的な例外型を使用すること。

すべてを `Exception` で捕捉しないこと。

---

# 11. 型ヒントのルール

関数の引数と戻り値には、理解しやすい範囲で型ヒントを追加すること。

例：

```python
from pathlib import Path

def load_sales_csv(csv_path: Path) -> pandas.DataFrame:
    ...
```

例：

```python
def validate_target_month(target_month: str) -> list[str]:
    ...
```

ただし、次の変更は避けること。

* 型ヒント追加だけを目的とした大規模変更
* 初学者が理解しにくい複雑なジェネリクス
* 必要性の低い独自型の大量追加
* 実行環境と互換性のない型構文の導入

現在のPythonバージョンを確認し、対応している構文を使用すること。

---

# 12. ファイルパスのルール

ファイルパスの操作には、可能な範囲で `pathlib.Path` を使用すること。

推奨例：

```python
from pathlib import Path

input_directory = Path("input")
sales_csv_path = input_directory / "sales.csv"
```

避ける例：

```python
sales_csv_path = input_directory + "/" + file_name
```

Mac、Windows、Linuxで動作しやすい実装を維持すること。

ただし、既存コード全体への影響が大きい場合は、段階的な変更とすること。

---

# 13. Excel出力のルール

## 13.1 既存レイアウトを変更しない

ユーザーから明確な指示がない限り、次を変更しないこと。

* Excelテンプレート
* セル番地
* 行や列の追加・削除
* 結合セル
* 罫線
* フォント
* 背景色
* 列幅
* 行の高さ
* タイトル
* 出力項目
* 出力順序

---

## 13.2 現在の主要セル配置

現在の報告書仕様は次のとおり。

| セル  | 項目             |
| --- | -------------- |
| A1  | タイトル           |
| B3  | 対象年月           |
| D3  | 店舗コードまたはエリアコード |
| F3  | 店舗名またはエリア名     |
| B6  | 税抜売上           |
| D6  | 消費税            |
| F6  | 税込売上           |
| B9  | 来店客数           |
| D9  | 客単価            |
| F9  | 消費税率           |
| B12 | 原価             |
| D12 | 人件費            |
| F12 | 家賃             |
| B15 | 光熱費            |
| D15 | その他経費          |
| F15 | 店舗運営費用         |
| B18 | 営業利益           |
| D18 | 原価率            |
| F18 | 人件費率           |
| B21 | 営業利益率          |

実際のコードおよびテンプレートと異なる場合は、独断で変更せず、差異を報告すること。

営業利益率のセルは、実装上のセル番地を必ず確認すること。

---

## 13.3 数値と表示形式を分離する

Python側では計算に適した数値を保持し、Excel側では表示形式を設定すること。

例：

* 金額：数値として書き込む
* 比率：小数値として書き込む
* パーセント表示：Excelの `number_format` で設定する

文字列として `"30%"` を書き込む方法は、既存仕様上の理由がない限り採用しないこと。

---

# 14. テストのルール

## 14.1 変更前に既存テストを実行する

可能な場合は、変更前に既存テストを実行し、現在の状態を確認すること。

例：

```bash
pytest
```

またはプロジェクトで定められた実行方法を使用すること。

テストが変更前から失敗している場合は、その事実を記録すること。

変更前から存在する失敗を、今回の変更による失敗として扱わないこと。

---

## 14.2 変更後にテストを実行する

各変更後に、関連するテストを実行すること。

最後に、可能な限りテスト全体を実行すること。

例：

```bash
pytest tests/test_aggregator.py
pytest tests/test_validator.py
pytest
```

実際のテストファイルの場所に合わせること。

---

## 14.3 テストを通すためだけに期待値を変更しない

実装変更によってテストが失敗した場合は、まず実装が既存仕様を壊していないか確認すること。

次の行為は禁止する。

* 理由なく期待値を変更する
* 失敗するテストを削除する
* テストをスキップする
* 検証内容を弱くする
* 例外が発生しなくなっただけで正常と判断する
* 実装に合わせて仕様を変更する

テストの変更が必要な場合は、その理由を説明すること。

---

## 14.4 対象年月のテスト

対象年月の動的指定について、少なくとも次を確認すること。

* 正しい `YYYYMM` を受け付ける
* 6桁未満を拒否する
* 6桁を超える値を拒否する
* 数字以外を拒否する
* `00` 月を拒否する
* `13` 月を拒否する
* ハイフン形式を拒否する
* スラッシュ形式を拒否する
* 引数未指定時の動作を確認する
* 不正値の場合に報告書作成を開始しない

---

## 14.5 営業利益率のテスト

営業利益率について、少なくとも次を確認すること。

### 通常の利益

```python
net_sales = 1_000_000
operating_profit = 300_000
```

期待値：

```text
0.3
```

30や3000ではなく、小数値の0.3になること。

浮動小数点の比較には、必要に応じて `pytest.approx` を使用する。

例：

```python
assert operating_profit_margin == pytest.approx(0.3)
```

### 売上が0

売上が0でも、`ZeroDivisionError` が発生しないこと。

### 赤字

営業利益がマイナスの場合、営業利益率も負の値になること。

例：

```python
net_sales = 1_000_000
operating_profit = -100_000
```

期待値：

```text
-0.1
```

### 利益が0

営業利益が0の場合、営業利益率が0になること。

### 各集計単位

次で同じ計算ルールが使用されること。

* 店舗別
* エリア別
* 全体

---

## 14.6 Excel出力のテスト

可能な範囲で、生成されたExcelファイルを読み直して次を確認すること。

* 営業利益率セルに小数値が保存されている
* 30%の場合、保存値が約0.3である
* セルの表示形式がパーセント形式である
* 原価率の表示形式が正しい
* 人件費率の表示形式が正しい
* 店舗報告書で正しい
* エリア報告書で正しい
* 全体報告書で正しい

テンプレートを使用する場合は、テンプレート側の表示形式も確認すること。

---

## 14.7 テスト名

テスト名から条件と期待結果が分かるようにすること。

推奨例：

```python
def test_validate_target_month_accepts_valid_yyyymm():
    ...
```

```python
def test_validate_target_month_rejects_month_13():
    ...
```

```python
def test_operating_profit_margin_is_calculated_as_decimal():
    ...
```

```python
def test_operating_profit_margin_does_not_raise_when_sales_are_zero():
    ...
```

```python
def test_report_writer_uses_percentage_format_for_operating_profit_margin():
    ...
```

避ける例：

```python
def test_data():
    ...
```

```python
def test_1():
    ...
```

---

# 15. Codexの作業単位

一度に対象ファイルをすべて変更しないこと。

原則として、関連性の高い1〜3ファイル単位で進めること。

推奨する作業順序は次のとおり。

## 第1段階：集計と比率計算

対象：

* `aggregator.py`
* `test_aggregator.py`

確認内容：

* 売上集計
* 営業利益の計算
* 原価率
* 人件費率
* 営業利益率
* ゼロ除算
* 店舗・エリア・全体の計算方法

---

## 第2段階：Excel出力

対象：

* `report_writer.py`
* 関連するテスト

確認内容：

* Excelへ書き込む値
* パーセント表示形式
* 店舗報告書
* エリア報告書
* 全体報告書
* テンプレートの既存書式

---

## 第3段階：対象年月の動的指定

対象：

* `main.py`
* `config.py`
* 関連するテスト

確認内容：

* コマンドライン引数
* `YYYYMM` 検証
* エラーメッセージ
* 処理開始前の入力チェック
* 既存処理への受け渡し

---

## 第4段階：CSVとマスタ読み込み

対象：

* `csv_loader.py`
* `master_loader.py`
* 関連するテスト

確認内容：

* ファイルパス
* データ型
* エラー処理
* 重複処理
* コメント
* 関数名
* 戻り値

---

## 第5段階：バリデーション

対象：

* `validation_rules.py`
* `validator.py`
* `test_validator.py`

確認内容：

* 責務の分離
* 検証順序
* エラー収集
* エラーメッセージ
* 既存検証ルールの維持
* 重複処理

---

## 第6段階：全体確認

確認内容：

* すべてのテスト
* 実際の報告書生成
* 店舗報告書
* エリア報告書
* 全体報告書
* 対象年月
* 数値項目
* 比率項目
* 出力ファイル名
* エラーハンドリング
* 未使用コード
* 不要なimport

各段階が完了する前に、無関係な次段階の変更を混在させないこと。

---

# 16. 各作業の進め方

Codexは、各作業で次の順序を守ること。

1. 対象ファイルと関連ファイルを読む
2. 現在の処理の流れを説明する
3. 問題点と改善候補を整理する
4. 今回変更する範囲を明示する
5. 仕様変更にならないことを確認する
6. 変更を実施する
7. 関連テストを実行する
8. 必要に応じてテストを追加する
9. テスト結果を報告する
10. 変更内容を初学者向けに説明する
11. 次の作業候補を示す
12. ユーザーの確認なしにコミットやプッシュを行わない

---

# 17. Codexが禁止される作業

ユーザーから明確な指示がない限り、次を行わないこと。

* 既存機能の削除
* 指定されていない新機能の追加
* CSVフォーマットの変更
* マスタCSVの項目変更
* Excelテンプレートの変更
* Excel出力セルの変更
* レポート項目の追加・削除
* 税率計算ルールの変更
* 売上集計ルールの変更
* 費用計算ルールの変更
* 出力ファイル名ルールの変更
* バリデーションルールの削除
* 外部ライブラリの追加
* フレームワークの導入
* ディレクトリ構成の大幅な変更
* すべての処理のクラス化
* 非同期処理への変更
* デザインパターンの導入
* データベースの導入
* GUIの追加
* Webアプリ化
* テストを通すためだけの期待値変更
* 失敗するテストの削除
* ユーザーの確認なしでのコミット
* ユーザーの確認なしでのプッシュ
* ユーザーの確認なしでのPull Request作成
* ユーザーの確認なしでのブランチ削除
* ユーザーの確認なしでのファイル削除

---

# 18. 不具合修正のルール

リファクタリング中に別の不具合を発見した場合は、勝手に修正しないこと。

次の順序で対応すること。

1. 不具合の内容を確認する
2. 再現条件を確認する
3. 原因を特定する
4. 影響範囲を確認する
5. 今回の作業対象との関連を説明する
6. ユーザーへ報告する
7. 修正の了承後に対応する
8. 再現テストを追加する
9. 修正後のテストを実行する

今回、修正が事前承認されている不具合は次のとおり。

* 営業利益率が3000%以上で表示される問題
* 営業利益率に関連するゼロ除算
* 原価率と人件費率に同様の二重変換がある場合の修正
* `target_month` が固定値になっている問題

これら以外の不具合は、修正前に報告すること。

---

# 19. 変更結果の報告形式

各作業の終了時に、次の形式で報告すること。

## 変更したファイル

変更したファイル名を列挙する。

## 変更前の状態

変更前にどのような処理・問題があったかを説明する。

## 変更内容

何をどのように変更したかを説明する。

## 変更理由

なぜ変更が必要だったかを説明する。

## 変更しなかった箇所

確認したが、変更不要と判断した箇所を説明する。

## 使用した主なPython機能

今回使用した構文や標準ライブラリを説明する。

例：

* `argparse`
* `pathlib.Path`
* 型ヒント
* docstring
* 例外処理
* ヘルパー関数
* `pytest.approx`

## テスト内容

実行したテストコマンドを記載する。

例：

```bash
pytest tests/test_aggregator.py
```

## テスト結果

成功数、失敗数、スキップ数を記載する。

例：

```text
8 passed
```

失敗がある場合は、失敗内容と今回の変更との関連を説明する。

## 影響範囲

次への影響を説明する。

* CSV読み込み
* マスタ読み込み
* 集計結果
* バリデーション
* Excel出力
* 店舗報告書
* エリア報告書
* 全体報告書
* 既存テスト

## 残っている課題

今回の作業範囲外とした改善候補があれば記載する。

## 処理の流れ

変更後の処理がどの順番で実行されるか、初学者にも分かるように説明する。

---

# 20. Pull Request用description

各機能または関連ファイルを `main.py` に組み込んだ段階で、Pull Request用descriptionを作成すること。

次の項目を含める。

## 背景

なぜ変更が必要だったか。

## 対応内容

どのファイルを、どのように変更したか。

## 確認内容

実施したテストと動作確認。

## 影響範囲

既存機能や関連モジュールへの影響。

必要に応じて次も含める。

* 修正前の不具合
* 修正後の動作
* 対象外とした内容
* 今後の課題

Pull Requestの作成自体は、ユーザーから明確な指示を受けた場合のみ行うこと。

---

# 21. 学習目的の説明ルール

この作業は、コードを修正するだけでなく、ユーザーがコードの流れを理解できることも目的とする。

変更後は、初学者にも理解できる表現で次を説明すること。

* 対象ファイルの役割
* 変更前の問題点
* 変更後の改善点
* 関数の役割
* 変数の役割
* 処理の実行順序
* 使用した英単語の意味
* 使用したPython構文
* なぜその実装を選んだか
* 他の実装方法との違い
* 共通化した理由
* 共通化しなかった理由
* テストで確認している内容
* 他ファイルへの影響

専門用語を使用する場合は、簡単な意味も併記すること。

コードだけを提示して説明を省略しないこと。

---

# 22. 完了条件

以下をすべて満たした場合に、今回のリファクタリングおよび機能修正を完了とする。

## コード品質

* 対象ファイルの役割が分かりやすい
* 関数の責務が明確である
* 処理の流れを追いやすい
* 分かりにくい変数名が整理されている
* 分かりにくい関数名が整理されている
* 必要な箇所にコメントがある
* 必要な関数にdocstringがある
* 不要なコメントがない
* 不要な重複処理が整理されている
* 共通化しすぎていない
* 不要なimportがない
* 未使用コードが増えていない

## 対象年月

* `target_month` の固定値が廃止されている
* 実行時に対象年月を指定できる
* `YYYYMM` 形式を検証できる
* `01` から `12` の月だけを受け付ける
* 不正な対象年月で処理を開始しない
* 利用者が原因を理解できるエラーを表示する
* 対象年月のテストが通る

## 営業利益率

* 営業利益率が正しい式で計算される
* 30%相当の値を小数値0.3として保持する
* Excelで30%程度と表示される
* 3000%と表示されない
* PythonとExcelで二重に100倍されていない
* 売上0でもゼロ除算にならない
* 赤字の場合に負の営業利益率になる
* 店舗・エリア・全体で計算方法が統一されている
* 営業利益率のテストが通る

## 関連比率

* 原価率に二重変換がない
* 人件費率に二重変換がない
* Excelの表示形式が適切である
* 問題がなければ不要な変更をしていない

## 既存仕様

* 既存のCSV読み込みが動作する
* 既存のマスタ読み込みが動作する
* 店舗別集計結果が維持されている
* エリア別集計結果が維持されている
* 全体集計結果が維持されている
* Excelの出力項目が変わっていない
* Excelのセル位置が変わっていない
* 出力ファイル名ルールが変わっていない
* 既存バリデーションが維持されている
* 既存エラーハンドリングが壊れていない

## テスト

* 変更前の既存テストが通る
* 変更した処理に必要なテストが追加されている
* 関連テストがすべて通る
* 可能な範囲でテスト全体が通る
* テストを通すためだけの仕様変更をしていない
* テスト結果が報告されている

## ユーザー理解

* 変更内容が初学者向けに説明されている
* 処理の流れが説明されている
* 使用したPython機能が説明されている
* 変更理由が説明されている
* 影響範囲が説明されている
* ユーザー自身が変更内容を説明できる状態になっている

---

# 23. 最終原則

このプロジェクトでは、最新技術や高度な設計を導入することよりも、次を優先する。

1. 既存仕様を壊さないこと
2. 正しい報告書を出力すること
3. コードの流れを理解しやすいこと
4. 必要な変更だけを行うこと
5. 変更理由を説明できること
6. テストで動作を確認できること
7. ユーザーがコードを読み返して理解できること

迷った場合は、大規模な変更を避け、現在の設計を生かした小さく安全な変更を選択すること。

修正を伴う作業では、変更内容を報告する前に必ず関連テストを実行すること。

さらに、可能な限りプロジェクト全体のテストも実行すること。

テストを実行できなかった場合は、その理由（環境不足、依存関係不足など）を必ず報告すること。

テスト結果は次の形式で報告すること。

- 実行したコマンド
- Passed
- Failed
- Skipped
- エラー内容（存在する場合）